.hero {
    text-align: center;
  padding: clamp(20px, 5vw, 80px);

}
h1{
 font-size: clamp(10px, 2rem, 3rem);
}
p {
    font-size: clamp(12px, 1.5rem, 2.5rem);
}
.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px,1fr));
    grid-template-rows: repeat(auto-fit, minmax(250px,1fr));
    gap: 20px;
    grid-auto-flow: dense;
}
.card {
    background: black;
    color: white;
    text-align: center;
    padding: 40px;
}
.card:nth-child(6) {
    /* grid-column: span 2;
    grid-row: span 2; */
}